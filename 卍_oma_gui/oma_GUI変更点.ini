v_0.14
    ssi_basic の ord_min, ord_step 解析でを使わないので削除
    ssi_find に ord_min, ord_max, ord_step を追加

v_0.15
    ssi_basic の ord_max の初期値を 100->60 に変更
    _mpe_from_efdd_and_ssidat() を _post_process() に変更し、
        _mpe_from_efdd_and_ssidat() の中身をいくつかの関数に分割
    各クラスターから外れ値を削除するようにした

v_0.16
    クラスター分析の閾値に mean を追加。mean では閾値を distant_matrix の
        平均と標準偏差から計算する

v_0.17
    mean での閾値を min_distance の平均と標準偏差から計算する

v_0.18
    mean での閾値を、ある極とより model order の低い極との最小距離をある極の最小距離とし、
        すべての極の最小距離の平均と標準偏差を用いて計算する

v_0.19.1
    single_setup (ss) の filtering をするときに、コマンドが正しくなかったのを修正
        ss.freq_filter_data() -> ss.filter_data()

    self.hierarchical_clustering() にで変なところ(r"D:\df_poles.csv")
        に csv を保存していたのを削除

v_0.20
    mean での極間の最小距離の求めるときに、the continuous-time eigenvalue の相対誤差とMAC値の差
        を使うようにする